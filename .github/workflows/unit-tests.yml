name: Tests

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  tests:
    name: Unit Tests
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - id: action
        uses: ./
        with:
          commands: |
            mvn -B verify -P all-tests checkstyle:checkstyle -Dcheckstyle.skip=false
          dir: backend
          java-distribution: temurin
          java-version: "17"
          repository: bcgov/nr-forest-client
          sonar_args: >
            -Dsonar.exclusions=**/coverage/**
            -Dsonar.organization=bcgov-nr
            -Dsonar.projectKey=action-test-and-analyse-java
          sonar_token: ${{ secrets.SONAR_TOKEN }}

      - name: Verify triggered output
        run: |
          echo "🔍 Verifying triggered output"
          echo "Actual: ${{ steps.action.outputs.triggered }}"
          
          # Check that the output is a valid boolean string
          if [ "${{ steps.action.outputs.triggered }}" != "true" ] && [ "${{ steps.action.outputs.triggered }}" != "false" ]; then
            echo "❌ triggered output has invalid value: ${{ steps.action.outputs.triggered }}"
            echo "Expected: 'true' or 'false'"
            exit 1
          fi
          
          echo "✅ triggered output is valid: ${{ steps.action.outputs.triggered }}"
